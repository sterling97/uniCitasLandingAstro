---
/* FORMATO — no editar para cambios de contenido (texto en content.ts). */
import { content } from '../config/content';

const year = new Date().getFullYear();
---

<div style="max-width:1200px;margin:0 auto;padding:42px clamp(20px,5vw,72px);font-size:13px;color:var(--color-neutral-600)">© {year} {content.brand.name} · {content.footer.note}</div>
